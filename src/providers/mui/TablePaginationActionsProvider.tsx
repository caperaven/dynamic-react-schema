import TablePagination from '@mui/material/TablePagination';

const TablePaginationActionsProvider = (props: any) => {
  // This is a placeholder for custom TablePagination actions if needed
  return <TablePagination {...props} />;
};

export default TablePaginationActionsProvider;
