import AccordionActions from '@mui/material/AccordionActions';

const AccordionActionsProvider = (props: any) => {
  return <AccordionActions {...props} />;
};

export default AccordionActionsProvider;
