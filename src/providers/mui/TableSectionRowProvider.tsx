import TableRow from '@mui/material/TableRow';

const TableSectionRowProvider = (props: any) => {
  // TableSectionRow is not a direct MUI component, but for completeness:
  return <TableRow {...props} />;
};

export default TableSectionRowProvider;
