import AccordionDetails from '@mui/material/AccordionDetails';

const AccordionDetailsProvider = (props: any) => {
  return <AccordionDetails {...props} />;
};

export default AccordionDetailsProvider;
