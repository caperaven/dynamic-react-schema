import AccordionSummary from '@mui/material/AccordionSummary';

const AccordionSummaryProvider = (props: any) => {
  return <AccordionSummary {...props} />;
};

export default AccordionSummaryProvider;
