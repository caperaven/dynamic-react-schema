import TableCell from '@mui/material/TableCell';

const TableFooterCellProvider = (props: any) => {
  // TableFooterCell is not a direct MUI component, but for completeness:
  return <TableCell {...props} />;
};

export default TableFooterCellProvider;
