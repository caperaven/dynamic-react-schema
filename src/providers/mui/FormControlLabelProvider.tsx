import FormControlLabel from '@mui/material/FormControlLabel';

const FormControlLabelProvider = (props: any) => {
  return <FormControlLabel {...props} />;
};

export default FormControlLabelProvider;
