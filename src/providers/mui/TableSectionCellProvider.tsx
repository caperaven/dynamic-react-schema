import TableCell from '@mui/material/TableCell';

const TableSectionCellProvider = (props: any) => {
  // TableSectionCell is not a direct MUI component, but for completeness:
  return <TableCell {...props} />;
};

export default TableSectionCellProvider;
